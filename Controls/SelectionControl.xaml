<UserControl x:Class="DiplomProject.Controls.SelectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiplomProject.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Выберите модель:"/>
        <ComboBox Grid.Row="1" Name="ModelComboBox" Margin="0,5,0,10"/>

        <Label Grid.Row="2" Content="Options:"/>
        <StackPanel Grid.Row="3" Margin="0,5,0,10">
            <CheckBox Name="GenerateViewModelCheckBox" Content="Создание ViewModel" Margin="0,2"/>
            <CheckBox Name="UseDataBindingCheckBox" Content="Используйте привязку данных" Margin="0,2"/>
            <CheckBox Name="AddValidationCheckBox" Content="Добавить проверку" Margin="0,2"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" Content="Ок" Width="80" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Name="CancelButton" Content="Отмена" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
